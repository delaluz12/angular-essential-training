import { Component } from "@angular/core";

// to decorate a component you need to add at least 2 meta-data properties as a min --- selector and template
@Component({
  selector: "app-root",
  template: "<h1>My-App</h1>",
})
export class AppComponent {}
